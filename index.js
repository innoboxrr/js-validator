/**
 * innoboxrr-js-validator
 *
 * Validacion de formularios en el navegador, a partir del atributo
 * `data-validators` que emiten los componentes de innoboxrr-form-elements y su
 * gemelo React.
 */

export { default, default as JSValidator } from './src/JSValidator.js'
export { defaultLimits, defaultMessages, rules } from './src/rules.js'
